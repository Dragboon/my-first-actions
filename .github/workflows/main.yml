name: CI
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        run: actions/checkout@v4
      - name: Hello Github Actions
        run: echo Hello, Github Actions!
